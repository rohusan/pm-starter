export function addTextToBody(text) {
  // const div = document.createElement('div');
  const oReplace = document.getElementById('jsSzReplace');
  // div.textContent = text;
  oReplace.innerHTML = text;
}